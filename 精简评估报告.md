# SmsForwarder 精简收益评估

> 目标：① 发送通道只留「邮箱 + Server酱」；② 只保留「通知转发」核心能力（短信 / 来电 / APP 通知）
> 统计口径：`app/src/main/kotlin` + `app/src/main/res`，行数为物理行数（含空行注释），路径 `cn/ppps/forwarder/`

---

## 〇、改造结果（已执行）

| 指标 | 改造前 | 改造后 | 变化 |
|---|---:|---:|---:|
| Kotlin 文件数 | 339 | **157** | -182 |
| Kotlin 行数 | 47,135 | **19,988** | -27,147（-57.6%） |
| 布局文件数 | 101 | **36** | -65 |
| 布局行数 | 21,033 | **5,401** | -15,632 |
| drawable 资源 | 155 | **64** | -91 |
| `app/libs` | 21.9 MB | **0** | 移除 `frpclib.aar` |
| 删除文件合计 | — | **346 个** | — |
| Kotlin+布局行数 | 68,168 | **25,389** | **-42,779（-62.8%）** |

### 通道现状（3 个）

| 通道 | type | 说明 |
|---|---:|---|
| 邮箱 | 1 | 保留，含 PGP / S-MIME / OpenKeychain |
| Server酱·Turbo | 6 | 保留 |
| **微信（本地网关）** | **16** | **新增**，`http://{host}:{port}/v1/wechat`，host/port/token 在通道配置页填写 |

微信通道支持三种请求方式（与网关 API 一一对应）：
标准 JSON POST、裸文本 POST（`text/plain`）、GET（token 与 text 走 query）；
POST 走 `Authorization: Bearer <token>`。标题模板留空则只推正文，填写后按「标题\n正文」拼接。

### 已移除的功能模块

| 模块 | 处理方式 |
|---|---|
| 自动任务（任务/条件/动作/Worker） | 整体删除，数据库 `Task` 表随 MIGRATION_21_22 删除 |
| 主动控制（客户端 / 服务端 HTTP API） | 整体删除 |
| 内网穿透 FRPC | 整体删除，含 `frpclib.aar` 与动态下载逻辑 |
| Tinker 热修复 | 删除（仅用于加载 frpc 的 .so） |
| 定位服务 | 删除，`{{LOCATION}}` 系列模板标签一并移除 |
| 在线更新 XUpdate | 删除 |
| 短信指令 | 删除 |
| Logcat 日志抓取页 | 删除（原项目中已无入口） |
| 震动 / 闪光灯提醒 | 删除（仅服务于自动任务的「声音警报」动作） |
| 保活 Cactus | **保留** |

### 保留的「外围」代码（有意为之）

- `utils/task/TaskUtils.kt`：原为自动任务的状态类，现退化为**设备状态缓存**，供消息模板的
  `{{电池电量}}` `{{IPV4}}` `{{IPV6}}` `{{IP_LIST}}` 等标签读取。
- `receiver/BatteryReceiver.kt` / `receiver/NetworkChangeReceiver.kt`：已删除触发自动任务的分发逻辑，
  只保留刷新 `TaskUtils` 的部分，以免模板标签失效。

### 数据库

`AppDatabase` 版本 21 → **22**，新增 `MIGRATION_21_22`：

```kotlin
database.execSQL("DROP TABLE IF EXISTS `Task`")
database.execSQL("DROP TABLE IF EXISTS `Frpc`")
```

`Sender.type` 沿用上游编号（邮箱=1、Server酱=6），新增微信=16，避免老数据错位；
老版本中其它 type 的通道记录会显示为通用图标，重新保存即可。

> ⚠️ 上游 `SendersFragment` 在「新增通道」时直接把列表下标当作 type 写入（注释写着「目前刚好是这个顺序而已」）。
> 通道精简后下标与 type 不再相等，已改为 `SENDER_TYPE_LIST` 显式映射，否则新建的通道会因 type 不匹配而
> fallback 到 `unknown_sender`。

### 依赖 / 权限 / 打包

- **移除依赖**：`frpclib.aar`、`andserver`（含 kapt processor）、`crondroid` + `cron-parser`、
  `com.github.pppscn:location`、`codeview`、`org.eclipse.paho:...mqttv3`
- **移除权限 13 项**：定位 ×3、蓝牙 ×5、精确闹钟、读写通话记录、写联系人、读账户、相机
- **Manifest 组件**：23 → 14
- `build.gradle` 去掉 andserver 插件、libs 目录与 frpc 的 `.so` 剔除逻辑

---

## 一、总量基线

| 项目 | 文件数 | 行数 |
|---|---:|---:|
| Kotlin 源码 | 339 | 47,135 |
| 布局 XML | 101 | 21,033 |
| **合计** | **440** | **68,168** |

---

## 二、通道精简：16 → 2（邮箱 + Server酱）

现有 16 个发送通道，`SendUtils.sendMsgSender()` 里是一个 `when(sender.type)` 大分支，
`SendersFragment` 里是一组 `PageInfo` 列表，每个通道 = Utils + Fragment + Setting + Result + Layout 五件套。

| 通道 | type | Utils | Fragment | Setting | Result | Layout | 小计 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 钉钉群机器人 | 0 | 130 | 250 | 22 | 0 | 300 | 702 |
| **邮箱 ✅ 保留** | 1 | 404 | 742 | 34 | 0 | 644 | **1,824** |
| Bark | 2 | 189 | 297 | 32 | 6 | 456 | 980 |
| Webhook | 3 | 336 | 339 | 36 | 0 | 431 | 1,142 |
| 企业微信群机器人 | 4 | 92 | 241 | 20 | 5 | 244 | 602 |
| 企业微信应用 | 5 | 209 | 310 | 30 | 11 | 488 | 1,048 |
| **Server酱 ✅ 保留** | 6 | 85 | 229 | 9 | 6 | 183 | **512** |
| Telegram | 7 | 187 | 271 | 40 | 6 | 370 | 874 |
| 手机短信（转发通道） | 8 | 75 | 258 | 28 | 0 | 187 | 548 |
| 飞书群机器人 | 9 | 210 | 261 | 22 | 6 | 307 | 806 |
| PushPlus | 10 | 99 | 253 | 14 | 7 | 333 | 706 |
| Gotify | 11 | 86 | 223 | 8 | 14 | 186 | 517 |
| 钉钉企业内机器人 | 12 | 222 | 284 | 36 | 12 | 424 | 978 |
| 飞书企业应用 | 13 | 171 | 266 | 34 | 11 | 332 | 814 |
| URL Scheme | 14 | 75 | 226 | 6 | 0 | 113 | 420 |
| Socket | 15 | 288 | 273 | 49 | 0 | 514 | 1,124 |

### 删除量

| 类别 | 文件 | 行数 |
|---|---:|---:|
| `utils/sender/*.kt`（删 14） | 14 | 2,382 |
| `fragment/senders/*.kt`（删 14） | 14 | 3,752 |
| `entity/setting/*.kt`（删 14） | 14 | 380 |
| `entity/result/*.kt`（删 11） | 11 | 101 |
| `res/layout/fragment_senders_*.xml`（删 14） | 14 | 4,685 |
| `drawable-xhdpi/icon_*.webp`（删 14） | 14 | ≈56 KB |
| **小计** | **81** | **11,300** |

### 边缘改动（约 -700 行）

- `utils/SendUtils.kt`：删 14 个 `when` 分支 → 约 -90 行（403 → 310）
- `utils/Constants.kt`：删 14 个 `TYPE_*` 常量 → -14 行
- `fragment/SendersFragment.kt`：删 14 个 `PageInfo` + 14 个 import → 约 -115 行
- `database/entity/Sender.kt` / `LogsDetail.kt`：各删 14 个图标分支 → 约 -60 行
- `res/values*/strings.xml` × 4 语言：每语言约 -105 条 → 约 -420 行
- `README.md` / `proguard-rules.pro` 零星同步

### 通道精简收益

- **代码：约 -12,000 行**（占全项目 17.6%），**-81 个文件**
- **依赖：可移除 `org.eclipse.paho:...mqttv3`**（全项目仅 `SocketUtils` 使用）
- **APK：约 -0.5 MB 级**（14 个 webp 图标 56 KB + R8 后代码 + MQTT 库）
- **维护成本**：以后新增一个通道 ≈ 800 行代码 + 3 套语言字符串，此成本不再存在

---

## 三、只做「通知转发」后，还能砍掉的模块

核心保留：短信/来电/APP 通知的接收（`SmsReceiver`/`PhoneStateReceiver`/`CallReceiver`/`NotificationService`）
→ 规则匹配（`rules`）→ 通道发送（`senders`）→ 日志（`logs`）。

以下是「与核心转发无耦合」的外围功能：

| 模块 | 内容 | Kotlin 文件 | Kotlin 行 | 布局文件 | 布局行 | 合计 | 备注 |
|---|---|---:|---:|---:|---:|---:|---|
| **自动任务** | 任务/条件/动作引擎 + 9 个 Worker | 56 | 11,111 | 24 | 7,490 | **18,601** | 最大块；含 crondroid 依赖 |
| **客户端/服务端** | 远程控制 HTTP API + Client UI | 36 | 3,933 | 11 | 1,923 | **5,856** | 含 andserver 依赖 |
| **内网穿透 FRPC** | 穿透配置 + 动态下载 so | 7 | 777 | — | — | **777** | 附带 `frpclib.aar` 21.9 MB |
| 保活 Cactus | 双进程/前台服务保活 | 4 | 507 | — | — | 507 | ⚠ 影响到达率，建议保留 |
| Tinker 热修复 | so 补丁加载 | 2 | 436 | — | — | 436 | 独立 |
| 定位服务 | LocationService | 3 | 313 | — | — | 313 | 独立 |
| 在线更新 | XUpdate | 5 | 246 | — | — | 246 | 独立 |
| 短信指令 | 远程指令（含 frpclib 调用） | 1 | 161 | — | — | 161 | 依赖 server + frpc |
| **小计（不含保活）** | | **110** | **16,977** | **35** | **9,413** | **26,390** | |

### 依赖与权限同步精简

可移除的 Gradle 依赖：

| 依赖 | 归属模块 |
|---|---|
| `frpclib.aar`（21.9 MB，仅构建期） | FRPC |
| `cn.ppps.andserver:api` + processor | 客户端/服务端 |
| `gatewayapps.crondroid` + `net.redhogs.cronparser` | 自动任务 |
| `com.github.pppscn:location` | 定位 |
| `io.github.amrdeveloper:codeview` | FRPC 编辑器 |
| `com.umeng.umsdk:common/asms/uyumao` | 统计（可选） |
| `org.eclipse.paho:...mqttv3` | Socket 通道 |
| `org.pgpainless` + `org.sufficientlysecure:openpgp-api` + `bcpkix` | 邮箱 PGP/S-MIME（不需要邮件加密则可去） |

可移除的权限（约 10 项）：

- 定位 3 项：`ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION` / `ACCESS_BACKGROUND_LOCATION`
- 蓝牙 5 项：`BLUETOOTH` / `BLUETOOTH_ADMIN` / `BLUETOOTH_CONNECT` / `BLUETOOTH_SCAN` / `BLUETOOTH_ADVERTISE`
- 精确闹钟 1 项：`SCHEDULE_EXACT_ALARM`
- 其他：`WRITE_CONTACTS` / `GET_ACCOUNTS`（客户端联系人）、`READ_CALL_LOG`（客户端通话查询）

---

## 四、合计收益

| 项目 | 文件 | 行数 | 占比 |
|---|---:|---:|---:|
| 通道精简（16→2） | 81 | ≈12,000 | 17.6% |
| 外围模块精简 | 145 | 26,390 | 38.7% |
| **合计** | **226** | **≈38,400** | **56.3%** |

精简后：Kotlin 47,135 → 约 23,500 行；布局 21,033 → 约 6,900 行；总规模 68,168 → 约 30,400 行。

---

## 五、建议执行顺序（风险从低到高）

1. **FRPC** — 最干净，顺带去掉 21.9 MB 的 aar 和动态下载逻辑
2. **通道精简** — 机械式删除，改一个 `when` 即可，无隐藏耦合
3. **短信指令 / 在线更新 / 定位 / Tinker** — 小模块，各自独立
4. **客户端/服务端** — 需同步清理 `server/` + `fragment/client/` + `HttpServerService`
5. **自动任务** — 体量最大、与 Rule 有弱耦合，放最后

> ⚠ 保活（Cactus）不建议删：它直接决定后台存活率和消息到达及时性。

---

## 六、需要注意的耦合点

- `utils/SmsCommandUtils.kt` 同时依赖 `HttpServerService` 和 `frpclib.Frpclib` → 删 server / frpc 时必须一起处理
- `App.kt` 在启动流程里集中初始化了 FRPC、Location、Bluetooth、HttpServer、Cactus 五个服务 → 删模块时要同步清理
- `Core.kt` 持有 `FrpcRepository` / `TaskRepository`，Room 数据库 `AppDatabase` 注册了对应 Entity → 删模块需处理数据库版本迁移（建议直接卸载重装或走 Migration）
- `database/entity/Sender.kt` 与 `LogsDetail.kt` 是通道 type → 图标/名称的映射点，漏改会在日志页崩溃
